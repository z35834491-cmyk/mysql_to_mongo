# 复制为 .env 后生效（与 docker-compose.yml 同目录，或在仓库根目录 .env 亦可，视 compose 工作目录而定）
#
# 开启数据同步测试栈（MySQL / Mongo RS / Redis / RabbitMQ）：
# COMPOSE_PROFILES=sync
#
# 然后直接：
#   docker compose -f infra/docker/docker-compose.yml up -d --build
