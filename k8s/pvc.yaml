apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shark-platform-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # storageClassName: standard # Uncomment if needed
